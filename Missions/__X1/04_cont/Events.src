On BuildingComplete(b) do
begin
if GetBType(b) = b_depot and GetSide(b) = 2 then
   baseConstructed := true;

if GetBType(b) = b_lab and GetSide(b) = 2 then
   labConstructed := true;
End;

On HiddenCameraPlaced(side, un, x, y) do
begin
display_strings := [side, un, x, y];
End;