{Every 0$1 do
var i;
begin
enable;

display_strings := [];

for i in FilterAllUnits([f_btype, b_depot]) do
    display_strings := display_strings ^ [GetResourceType(GetBase(i), mat_oil), GetResourceType(GetBase(i), 3)];
End;}
On OilExtracted(building, side, base, amount) do
begin

End;

Every 0$1 do
begin
HintSpec('VehLimit', 2);
PlaceMinesToArea(minesAr, 23, 10, 2, 0);
End;


