Export timer;
Every 1 trigger game_started do
begin
counter_start := true;
timer := 0$00;
End;

Every 0$01 trigger counter_start do
begin
enable;

     Display_Strings := ['#Ar06-1', timer, '#Ar06-2', dip_points];
     timer := timer + 0$01;

End;