Every 0$01 trigger Louis do
begin

     While(true) do
     begin
          music_nat := 1; // usa theme
          Wait(Rand(3$00, 5$00));

          music_nat := 2; // ar theme
          Wait(2$10);
          // repeat 
     end;

End;
