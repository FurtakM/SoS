//
Starting
begin
ResetFog;
End;

Export function SaveTech(side, saveId);
var i;
begin
result := [];

for i := 1 to 100 do
    result := Join(result, [i, GetTech(i, side)]);

/// SaveVariable(result, saveId);
End;

Export techs;
Every 0$1 do
begin
SetTech(88, 2, state_disabled);
End;