Every 1$25 trigger sikorskiInOmicron and GetResourceArea(cratesSpawnArea, mat_cans) < [200, 150, 120][Difficulty] do
begin
enable;

wait(rand(0$10, 0$20)); 
CreateCratesArea(rand(2, 5), cratesSpawnArea, true);
End;

Every 1$25 do
begin
enable;
CreateCratesArea(rand(2, 5), ruCratesSpawnArea, true);
End;