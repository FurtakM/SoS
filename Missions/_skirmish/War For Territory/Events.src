// Special sources for CPU
On BuildingComplete(b) do
begin
if GetSide(b) <> 1 and GetBType(b) = b_depot then
   SetResourceType(GetBase(b), mat_cans, 20);
End;

On LeaveBuilding(b, un) do
begin
if GetClass(un) = 1 and GetTag(un) > 0 then
   SetTag(un, 0);
End;