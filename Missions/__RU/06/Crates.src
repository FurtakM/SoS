Every 0$01 do
var timer;
begin
timer := 5$30;

    repeat
     wait(timer);

     CreateCratesArea(rand(3,5), CratesArea, true);
     cratesSpawned = cratesSpawned + 1;
    until cratesSpawned = 3;

end;